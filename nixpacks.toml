[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install poetry",
  "python3 -m poetry config virtualenvs.create false",
  "python3 -m poetry install --no-interaction --without dev"
]
